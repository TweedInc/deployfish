name: runner-test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Show runner info
        run: |
          echo "Runner hostname:"
          hostname
          echo "User:"
          whoami
          echo "OS:"
          uname -a
